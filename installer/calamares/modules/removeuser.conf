# Argon OS — remove the live 'argon' user from the installed system.
# The live user is baked into the image (see the 0150 live-user hook) so
# the live session always works; on install it must be removed, and the
# installer creates the user's real account instead.
---
username: argon
