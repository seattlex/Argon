# Argon OS — package operations after install
# Remove live-session-only packages from the installed system.
---
backend: apt

operations:
  - try_remove:
      - calamares
      - live-boot
      - live-config
      - live-config-systemd
