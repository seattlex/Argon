# iso/

Build output directory: `scripts/build-iso.sh` places finished images and
their checksums here. Contents are not committed (see `.gitignore`).

Published images live on the GitHub releases page.
